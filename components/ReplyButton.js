import React from 'react'
import Button from '@/componenents/Button'

export default function ReplyButton() {
    return (
        <Button 
            onClick={undefined}
            value="Reply"
        />
    )
}